import React from 'react'
import Form from './Form'

function Contacto() {
    return (
        <section id="contacto">
            <h2>Contacto</h2>
            <Form></Form>
        </section>
    );
};

export default Contacto;